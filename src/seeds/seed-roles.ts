// import { Role } from "@/entities";

const defaultRoles = [
	{
		name: "Super Admin",
		slug: "super-admin",
	},
	{
		name: "Member",
		slug: "member",
	},
];

export const seedRoles = (workspaceId: string, ownerId: string) => {
	// seed default roles of a workspace
};

// export default { seedRoles };
